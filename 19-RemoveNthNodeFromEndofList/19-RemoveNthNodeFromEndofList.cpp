        }
            fast = fast->next;
        for(int i = 0; i < n; ++i) {
        start->next = head;
        ListNode* slow = start;
        ListNode* fast = start;
        ListNode* start = new ListNode();
    ListNode* removeNthFromEnd(ListNode* head, int n) {
class Solution {
public:
[1,2,3,4,5]
